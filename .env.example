# Sample env (currently not read by code — settings live in the UI).
# Kept for future config-driven defaults.

PROJECT_NAME=GridWorld_2
PYTHONPATH=.

# Default hyperparameters (override in Settings menu)
GAMMA=0.9
ACCURACY=0.0001
STEP_COST=0.0
SLIP=0.0

# Grid
ROWS=4
COLS=4

# Render
FPS=60
